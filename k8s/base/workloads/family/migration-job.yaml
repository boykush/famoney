apiVersion: batch/v1
kind: Job
metadata:
  name: family-migration
  annotations:
    argocd.argoproj.io/hook: PreSync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation
spec:
  template:
    metadata:
      annotations:
        sidecar.istio.io/inject: "false"
    spec:
      containers:
      - name: migrate
        image: family-migration:latest
        env:
        - name: DATABASE_URL
          value: postgres://famoney:famoney-local-password@postgres-rw:5432/family_db?sslmode=disable
      restartPolicy: Never
  backoffLimit: 3
