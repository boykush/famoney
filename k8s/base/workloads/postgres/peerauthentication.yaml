apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: postgres-allow-plaintext
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: postgres
  mtls:
    mode: PERMISSIVE
