# Build artifacts
bin/
server/*/server

# mise local configuration (contains secrets)
.mise.local.toml

# Node.js
node_modules/
.next/

# apm install unpacks dependencies here; the files it generates (.mcp.json etc.) are committed
apm_modules/
